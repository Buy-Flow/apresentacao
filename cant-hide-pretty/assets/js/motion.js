(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const touch=matchMedia('(hover:none), (pointer:coarse)').matches;const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];
function boot(){
  if(!reduce){const bar=document.createElement('div');bar.className='motion-progress';bar.innerHTML='<span></span>';document.body.appendChild(bar);const progress=bar.firstElementChild;let ticking=false;addEventListener('scroll',()=>{if(ticking)return;ticking=true;requestAnimationFrame(()=>{const h=document.documentElement.scrollHeight-innerHeight;progress.style.transform=`scaleX(${h?scrollY/h:0})`;ticking=false})},{passive:true});}
  if(location.pathname.endsWith('/')||location.pathname.endsWith('/index.html')||/cant-hide-pretty\/?$/.test(location.pathname)){if(!reduce&&!sessionStorage.getItem('chp_intro_seen')){const intro=document.createElement('div');intro.className='motion-wordmark';intro.innerHTML='<div class="motion-wordmark-inner"><small>CAN’T HIDE</small><em>Pretty</em></div>';document.body.appendChild(intro);sessionStorage.setItem('chp_intro_seen','1');setTimeout(()=>intro.remove(),1250);}}
  const hero=q('.hero-copy-v3 h1');if(hero&&!hero.querySelector('.motion-line')){const parts=hero.innerHTML.split(/<br\s*\/?\s*>/i);hero.innerHTML=parts.map(x=>`<span class="motion-line"><span class="motion-line-inner">${x}</span></span>`).join('');}
  qa('.section-head,.signature-copy,.store-copy,.page-hero .wrap,.product-info,.content-card,.timeline,.newsletter-inner').forEach((el,i)=>{el.dataset.reveal='';if(i%3)el.dataset.revealDelay=String(i%3)});
  qa('.signature-art figure,.store-arch,.about-photo,.store-photo').forEach(el=>el.dataset.reveal='');
  const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');obs.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -7% 0px'});qa('[data-reveal],.manifesto-v3').forEach(el=>obs.observe(el));
  const cardObs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');cardObs.unobserve(e.target)}}),{threshold:.08});const observeCards=()=>qa('.pretty-edit-track .product-card,.editorial-grid .product-card').forEach(x=>cardObs.observe(x));observeCards();new MutationObserver(observeCards).observe(document.body,{childList:true,subtree:true});
  if(!reduce){const layers=[['.shape-a',-.018],['.shape-b',.025],['.hero-main',-.012],['.hero-mini',.02],['.hero-seal',-.022]];let raf=0;const update=()=>{const y=scrollY;layers.forEach(([s,d])=>{const el=q(s);if(el)el.style.setProperty('--par-y',`${Math.max(-28,Math.min(28,y*d))}px`)});raf=0};addEventListener('scroll',()=>{if(!raf)raf=requestAnimationFrame(update)},{passive:true});update();}
  qa('.pretty-edit-track').forEach(track=>{let down=false,start=0,left=0;track.addEventListener('pointerdown',e=>{if(touch)return;down=true;start=e.clientX;left=track.scrollLeft;track.setPointerCapture(e.pointerId);track.classList.add('dragging')});track.addEventListener('pointermove',e=>{if(!down)return;track.scrollLeft=left-(e.clientX-start)*1.25});const up=()=>{down=false;track.classList.remove('dragging')};track.addEventListener('pointerup',up);track.addEventListener('pointercancel',up)});
  if(!touch&&!reduce){const cursor=document.createElement('div');cursor.className='motion-cursor';cursor.textContent='VIEW';document.body.appendChild(cursor);let cx=-100,cy=-100,tx=-100,ty=-100;const loop=()=>{cx+=(tx-cx)*.18;cy+=(ty-cy)*.18;cursor.style.left=cx+'px';cursor.style.top=cy+'px';requestAnimationFrame(loop)};loop();addEventListener('mousemove',e=>{tx=e.clientX;ty=e.clientY;cursor.classList.add('on')});addEventListener('mouseleave',()=>cursor.classList.remove('on'));qa('.product-media,.mood-card,.hero-main,.store-arch').forEach(el=>{el.addEventListener('mouseenter',()=>{cursor.classList.add('active','big');cursor.textContent=el.classList.contains('mood-card')?'EXPLORE':'VIEW'});el.addEventListener('mouseleave',()=>{cursor.classList.remove('active','big');cursor.textContent='VIEW'})});}
  qa('a[href]').forEach(a=>a.addEventListener('click',e=>{if(reduce||e.defaultPrevented||e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;const u=new URL(a.href,location.href);if(u.origin!==location.origin||a.target==='_blank'||u.hash&&u.pathname===location.pathname)return;if(document.startViewTransition)return;e.preventDefault();document.body.classList.add('page-leave');setTimeout(()=>location.href=u.href,220)}));
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();})();
